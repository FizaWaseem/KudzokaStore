import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#25aae1',
     light: '#25aae1',
    },
    secondary: {
      main:  '#fff',
    },
    type:'light'
  },
});
export default theme;